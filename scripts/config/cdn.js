(function () {
    "use strict";

    // Static HTML: edit base here. Vite later: import.meta.env.VITE_CDN_BASE
    window.CDN_CONFIG = {
        base: "https://batest.bossarms.xyz",
        textureRoot: "textures",
        // Bump after uploading new textures to R2 (same paths); reload site to fetch fresh files.
        textureVersion: "4"
    };
})();
